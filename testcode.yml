---
- name: Network Getting Started First Playbook
  connection: network_cli
  become: yes
  hosts: all
  tasks: 
  
     - name: ensure that the desired
       cisco.ios.config: 
         commands:
          - hostname test 
